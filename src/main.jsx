import React from 'react';
import ReactDOM from 'react-dom/client';
import AnalizadorCV from '../analizador-cv.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AnalizadorCV />
  </React.StrictMode>,
);
